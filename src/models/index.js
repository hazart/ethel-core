const dialog = require('./dialog');
const user = require('./user');
const userDetail = require('./userDetail');
const userString = require('./userString');
const targetEmail = require('./targetEmail');

module.exports = {
  dialog,
  user,
  userDetail,
  userString,
  targetEmail,
};
